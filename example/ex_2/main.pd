str::const msg = "Test";
int count = 42;
float pi = 3.14;

sendln("Message: {{msg}}");
sendln("Count: {{count}}");
sendln("Pi: {{pi}}");
sendln("Combined: {{msg}} - {{count}} - {{pi}}");

if (count == 42) {
    sendln(count);
} elsif (x > 42) {
    sendln(count);
} else {
    sendln(count)
}
